package com.company.micro.config.interceptor;

import org.springframework.web.servlet.handler.HandlerInterceptorAdapter;

/**
 * Abstract class for interceptors which are common to all requests.
 */
public abstract class GlobalInterceptors extends HandlerInterceptorAdapter {
}
